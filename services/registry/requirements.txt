fastapi==0.141.1
uvicorn==0.52.4
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
redis==5.0.1
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.0
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.1.0
# websockets >= 13 ships the current asyncio client the SDK uses and the
# sans-I/O server core uvicorn 0.52 selects. 16.1.1 is what the combined pin
# set (requirements-dev.txt) resolves to — the image and CI now agree.
websockets==16.1.1
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-semantic-conventions==0.65b0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-sqlalchemy==0.65b0
python-dotenv==1.2.3
ed25519==1.5
httpx==0.28.1
# A2A 1.0 (ADR-0009): official SDK types, ProtoJSON, JSON-RPC/HTTP+JSON dispatchers
# and SSE framing. docs/A2A_BASELINE.md pins the protocol line and the resolved graph.
a2a-sdk[http-server]==1.1.5
jsonschema==4.20.0
python-ulid==1.1.0
email-validator>=2.0.0
docker==7.0.0
prometheus-client==0.20.0
structlog==24.1.0
alembic==1.13.1
# Society QA runs the candidate's acceptance tests with `python -m pytest`
# INSIDE the worker image (services/registry/app/society/engineering/qa.py);
# pytest.ini (asyncio_mode, --timeout) needs these three at runtime. Same
# pins as requirements-dev.txt — one resolver pass, one version everywhere.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-timeout==2.4.0
# ...and the public product the Society repairs is the dashboard: QA and the
# offline fitness evaluation import services/dashboard/app with THIS
# interpreter (services/dashboard/tests/test_public_surface.py), so the
# dashboard's runtime is pinned here at the dashboard's exact versions
# (tests/test_dashboard_runtime_parity.py). The registry app never imports them.
flask==3.1.3
requests==2.34.2
