# Development and test dependencies for AgentNet.
#
# This is the ONE authoritative test/dev set. It resolves every service's
# pinned requirements in a SINGLE pip pass, so a conflicting pin between two
# services fails `pip install -r requirements-dev.txt` loudly instead of a
# later `pip install -r services/<x>/requirements.txt` silently replacing an
# earlier service's version (Phase 2.6 §7 — that is how CI once ran the
# registry code on websockets 16 while the image shipped 12). CI installs
# exactly this file and then runs `pip check`; scripts/ci/check_service_envs.sh
# additionally proves each service in its own isolated environment.
#
#   pip install -r requirements-dev.txt
-r services/registry/requirements.txt
-r services/payment/requirements.txt
-r services/worker/requirements.txt
-r services/simulation/requirements.txt
-r services/dashboard/requirements.txt

# Testing — pytest-timeout makes @pytest.mark.timeout real (pytest.ini turns
# unknown marks into errors) and `timeout = 900` in pytest.ini caps every test.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-timeout==2.4.0
pytest-cov==7.1.0
PyYAML>=6.0  # compose topology guard tests

# Linting & formatting (advisory in CI, see .github/workflows/ci.yml)
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
